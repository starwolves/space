[package]
name = "console_commands"
version = "0.0.0"
edition = "2021"

[dependencies.bevy]
version = "0.7.0"
default-features = false

[dependencies]
networking = { path = "../networking" }
shared = { path = "../shared" }
pawn = { path = "../pawn" }
